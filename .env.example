

# Used by docker-compose.yml for local runs. 
DB_PASSWORD=warehouse

# HS256 signing key
JWT_SECRET=replace-me-with-at-least-32-characters-of-random

# 'dev' exposes POST /api/auth/dev-token for minting test tokens.
SPRING_PROFILES_ACTIVE=dev
